spring:
  datasource:
    url: jdbc:mysql://localhost:3306/libDB
    username: root
    password: secret-pwd
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create-drop
server:
  port: 8080
logging:
  level:
    root: INFO
application:
  issue:
    max-allowed-books: 2